# 🏗️ Arquitectura Nginx + Certbot para Stivenads

## Diagrama de Flujo

```
┌─────────────────────────────────────────────────────────────┐
│                    INTERNET (Usuarios)                      │
│                   IP Pública + Dominio                      │
└────────────────────────────┬────────────────────────────────┘
                             │
                    ┌────────▼────────┐
                    │   Firewall      │
                    │ :80 (HTTP)      │
                    │ :443 (HTTPS)    │
                    │ :22 (SSH)       │
                    └────────┬────────┘
                             │
                    ┌────────▼────────────────┐
                    │     NGINX (Proxy)      │
                    │   Reverse Proxy        │
                    │   SSL/TLS Terminatior  │
                    │   Load Balancer        │
                    └────────┬────────────────┘
                             │
                ┌────────────┴────────────────┐
                │                             │
        ┌───────▼────────┐          ┌────────▼──────┐
        │  Node.js API   │          │  Frontend     │
        │  :3000         │          │  Static Files │
        │  - /api/*      │          │  - index.html │
        │  - /health     │          │  - CSS/JS     │
        └───────┬────────┘          └────────┬──────┘
                │                             │
        ┌───────▼────────────────────────────▼──────┐
        │                                            │
        │         MongoDB Atlas (Cloud DB)          │
        │         - Clientes (leads)                │
        │         - Consultants                     │
        │         - Bookings                        │
        │                                            │
        └────────────────────────────────────────────┘
```

---

## Flujo de Requests

### Request HTTPS Típico

```
1. Usuario abre: https://ejemplo.com/api/consultar
                         ↓
2. Nginx recibe en puerto 443
   - Verifica certificado SSL (Let's Encrypt)
   - Decodifica HTTPS a HTTP
                         ↓
3. Nginx evalúa la ruta:
   - ¿Comienza con /api/ ? → Proxy a Node.js
   - ¿Archivo estático?    → Sirve del frontend
                         ↓
4. Nginx proxy a Node.js en localhost:3000
   - Agrega headers X-Forwarded-*
   - Recibe respuesta
                         ↓
5. Nginx codifica respuesta a HTTPS
   - Agrega headers de seguridad (HSTS, etc)
                         ↓
6. Usuario recibe respuesta HTTPS
```

### Request HTTP (Redirección)

```
Usuario: http://ejemplo.com
                         ↓
Nginx recibe en puerto 80
                         ↓
Nginx responde:
301 Redirect to https://ejemplo.com
                         ↓
Usuario sigue redirección a HTTPS
```

---

## Certificados SSL - Ciclo de Vida

```
┌──────────────────────────────────────────────────────────┐
│                    Let's Encrypt                         │
│              (Entidad Certificadora Gratuita)            │
└────────────────────────┬─────────────────────────────────┘
                         │
          ┌──────────────▼──────────────┐
          │  1. Validación de Dominio  │
          │  (DNS o HTTP Challenge)    │
          └──────────────┬──────────────┘
                         │
          ┌──────────────▼──────────────┐
          │  2. Emisión de Certificado │
          │  Válido por 90 días        │
          └──────────────┬──────────────┘
                         │
          ┌──────────────▼──────────────┐
          │  3. Almacenamiento         │
          │  /etc/letsencrypt/live/    │
          └──────────────┬──────────────┘
                         │
          ┌──────────────▼──────────────┐
          │  4. Nginx Carga Certificado│
          │  Sirve HTTPS               │
          └──────────────┬──────────────┘
                         │
          ┌──────────────▼──────────────┐
          │  5. Cron Job (Día 60)      │
          │  Intenta renovar           │
          └──────────────┬──────────────┘
                         │
          ┌──────────────▼──────────────┐
          │  6. Nginx Recarga          │
          │  Nuevo certificado activo  │
          └──────────────────────────────┘
```

---

## Estructura de Directorios

```
/home/ubuntu/landingStiven/
├── api/                        # Backend Node.js
│   ├── index.js               # Punto de entrada
│   ├── booking/
│   ├── calendar/
│   └── ...
├── frontend/
│   ├── dist/                  # Archivos compilados (servidos por Nginx)
│   ├── src/
│   └── ...
├── nginx-template.conf        # Plantilla de configuración
├── setup-nginx.sh             # Script de instalación
├── configure-nginx.sh         # Script de configuración
├── setup-auto-renewal.sh      # Script de renovación automática
├── validate-setup.sh          # Script de validación
└── ...

/etc/nginx/
├── nginx.conf                 # Configuración principal
├── sites-available/
│   └── stivenads              # Configuración del sitio
├── sites-enabled/
│   └── stivenads → sites-available/stivenads
└── ...

/etc/letsencrypt/
├── live/
│   └── ejemplo.com/
│       ├── cert.pem           # Certificado
│       ├── privkey.pem        # Clave privada
│       ├── chain.pem          # Cadena de certificados
│       └── fullchain.pem      # Certificado completo (usado por Nginx)
├── archive/                   # Versiones anteriores
└── renewal/
    └── ejemplo.com.conf       # Configuración de renovación

/opt/certbot/
├── bin/
│   ├── python
│   ├── pip
│   └── certbot                # Ejecutable de Certbot
└── ...

/var/log/
├── nginx/
│   ├── stivenads_access.log   # Logs de acceso
│   ├── stivenads_error.log    # Logs de error
│   └── ...
└── letsencrypt/
    └── letsencrypt.log        # Logs de Certbot
```

---

## Flujo de Configuración Inicial

```
┌─────────────────────────────────────┐
│  1. Ejecutar setup-nginx.sh         │
│  - Instala Nginx                    │
│  - Instala Python + venv            │
│  - Instala Certbot                  │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  2. Ejecutar configure-nginx.sh     │
│  - Crea /etc/nginx/sites-available/ │
│  - Configura proxy a Node.js        │
│  - Recarga Nginx                    │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  3. Ejecutar certbot --nginx        │
│  - Valida dominio                   │
│  - Obtiene certificado SSL          │
│  - Configura HTTPS en Nginx         │
│  - Recarga Nginx                    │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  4. Ejecutar setup-auto-renewal.sh  │
│  - Configura cron job               │
│  - Prueba renovación (dry-run)      │
│  - Configura post-hook              │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  5. Ejecutar validate-setup.sh      │
│  - Verifica todo funciona           │
│  - Reporte de estado                │
└──────────────────────────────────────┘
```

---

## Seguridad en Capas

```
┌──────────────────────────────────────┐
│  Capa 1: Firewall                   │
│  - Solo puertos 80, 443, 22         │
│  - Bloquea todo lo demás            │
└──────────────────────────────────────┘
              ↓
┌──────────────────────────────────────┐
│  Capa 2: Nginx - Rate Limiting       │
│  - Limita requests por IP           │
│  - Previene DoS                     │
└──────────────────────────────────────┘
              ↓
┌──────────────────────────────────────┐
│  Capa 3: SSL/TLS Encryption         │
│  - HTTPS obligatorio                │
│  - Certificado Let's Encrypt        │
│  - TLS 1.2+ solamente               │
└──────────────────────────────────────┘
              ↓
┌──────────────────────────────────────┐
│  Capa 4: Headers de Seguridad       │
│  - HSTS                             │
│  - X-Frame-Options                  │
│  - Content-Security-Policy          │
└──────────────────────────────────────┘
              ↓
┌──────────────────────────────────────┐
│  Capa 5: API Authentication         │
│  - Validación en Node.js            │
│  - JWT Tokens                       │
│  - CORS configurado                 │
└──────────────────────────────────────┘
              ↓
┌──────────────────────────────────────┐
│  Capa 6: Base de Datos              │
│  - MongoDB Atlas (cloud)            │
│  - Encriptación en tránsito         │
│  - Backups automáticos              │
└──────────────────────────────────────┘
```

---

## Monitoreo y Mantenimiento

```
┌─────────────────────────────────────────┐
│        Sistema de Monitoreo             │
└──────────────────┬──────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
    ┌───▼───┐            ┌───▼────┐
    │ Diario│            │ Semanal│
    └───┬───┘            └───┬────┘
        │                     │
    - Revisar logs         - Backup
    - Certificados         - Actualizar
    - Procesos             - Logs
                             
    ┌───┬────────────────────┬───┐
    │ Mensual                     │
    ├─────────────────────────────┤
    │ - Estadísticas de uptime   │
    │ - Análisis de performance  │
    │ - Limpieza de logs         │
    │ - Renovación de certs      │
    └─────────────────────────────┘
```

---

## Variables de Entorno

```bash
# En /home/ubuntu/landingStiven/.env

# API
NODE_ENV=production
PORT=3000

# Base de Datos
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/stivenads

# Google Calendar
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...

# Email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=...
SMTP_PASS=...

# Dominio
DOMAIN=ejemplo.com
SSL_CERT=/etc/letsencrypt/live/ejemplo.com/fullchain.pem
SSL_KEY=/etc/letsencrypt/live/ejemplo.com/privkey.pem
```

---

## Performance Optimization

```
┌─────────────────────────────────────┐
│  Proxy Buffering                    │
│  - Cache responses en Nginx         │
│  - Reduce carga en Node.js          │
└──────────────────┬──────────────────┘

┌──────────────────▼──────────────────┐
│  Gzip Compression                   │
│  - Reduce tamaño de transferencia   │
│  - Automático en Nginx              │
└──────────────────┬──────────────────┘

┌──────────────────▼──────────────────┐
│  HTTP/2 Multiplexing                │
│  - Múltiples streams en 1 conexión  │
│  - Reduce latencia                  │
└──────────────────┬──────────────────┘

┌──────────────────▼──────────────────┐
│  Caching de Assets Estáticos        │
│  - 30 días para CSS/JS/Imágenes     │
│  - Reduce load del servidor         │
└──────────────────┬──────────────────┘

┌──────────────────▼──────────────────┐
│  Connection Pooling                 │
│  - Reutiliza conexiones             │
│  - Mejora throughput                │
└─────────────────────────────────────┘
```

---

## Escalabilidad Futura

```
Si necesitas escalar a múltiples servidores:

┌──────────────────────────────────┐
│    Load Balancer (Nginx)          │
│  - Distribuye tráfico            │
│  - Health checks                 │
└────────┬──────────────────────────┘
         │
    ┌────┴────┬──────────┐
    │          │          │
┌───▼──┐  ┌───▼──┐  ┌───▼──┐
│ API 1│  │ API 2│  │ API 3│
├──────┤  ├──────┤  ├──────┤
│Node.js  │Node.js  │Node.js
└────┬────┘────┬────┘────┬┘
     └────┬─────┴────┬────┘
          │          │
     ┌────▼──────────▼────┐
     │   MongoDB Atlas    │
     │   (Replicaset)     │
     └────────────────────┘

Implementable en AWS, DigitalOcean, etc.
```

---

**Última actualización**: Enero 2026
